<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="true" />
    <uses-feature android:name="android.hardware.sensor.gyroscope" android:required="true" />
    <uses-feature android:name="android.hardware.sensor.heartrate" android:required="false" />

    <!-- Frecuencia cardiaca. Sin este permiso la app mide golpeos pero no salud. -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <!-- Pasos y distancia del workout. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />

    <application
        android:name=".PadelWearApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <uses-library android:name="com.google.android.wearable" android:required="true" />

        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="false" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.DeviceDefault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            La sesión vive en un servicio en primer plano, no en la Activity: el jugador
            apaga la pantalla del reloj entre puntos y la medición no puede pararse.
        -->
        <service
            android:name=".service.PadelExerciseService"
            android:exported="false"
            android:foregroundServiceType="health" />

        <!--
            Recibe los ajustes que el jugador cambia en el móvil (mano, muñeca,
            sensibilidad, consentimiento de salud, modo de entrenamiento y alias).
            Google Play Services arranca el servicio aunque la app del reloj esté cerrada:
            los ajustes se cambian el día antes, no en la pista.
        -->
        <service
            android:name=".transport.SettingsListenerService"
            android:exported="true"
            android:permission="com.google.android.gms.permission.BIND_WEARABLE_LISTENER">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:pathPrefix="/padel/settings" />
            </intent-filter>
        </service>

        <!--
            El mando a distancia de las tandas: el móvil ordena, el reloj obedece. Va por
            mensaje y no por data item porque un data item se reentrega al reconectar, y
            una orden reentregada arrancaría una tanda que nadie ha pedido.
        -->
        <service
            android:name=".transport.TandaCommandService"
            android:exported="true"
            android:permission="com.google.android.gms.permission.BIND_WEARABLE_LISTENER">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:pathPrefix="/padel/mando-tanda" />
            </intent-filter>
        </service>

    </application>
</manifest>
